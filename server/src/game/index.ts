export { GameManager, gameManager } from './gameManager';
export { generateBoard } from './boardGenerator';
export { DEFAULT_CONFIG, PLAYER_COLORS } from './gameConfig';
export type { Cell, Player, GameRoom, Tool, GameConfig, CellState } from './types';
